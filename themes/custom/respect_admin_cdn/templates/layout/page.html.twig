<!DOCTYPE html>
<html lang="en">
  <head>
    <head-placeholder token="{{ placeholder_token }}">
    <title>{{ head_title|safe_join(' | ') }}</title>
    <css-placeholder token="{{ placeholder_token }}">
    
<!-- Tailwind via CDN -->
<script src="https://cdn.tailwindcss.com/3.4.16"></script>
<script>
  tailwind.config = {
    theme: {
      extend: {
        colors: { primary: "#4F46E5", secondary: "#F59E0B" },
        borderRadius: {
          none: "0px",
          sm: "4px",
          DEFAULT: "8px",
          md: "12px",
          lg: "16px",
          xl: "20px",
          "2xl": "24px",
          "3xl": "32px",
          full: "9999px",
          button: "8px",
        },
      },
    },
  };
</script>

    <js-placeholder token="{{ placeholder_token }}">
  </head>
  <body class="bg-gray-100 text-gray-900">
    <a href="#main-content" class="sr-only focus:not-sr-only">Skip to main content</a>
    <header class="bg-white shadow p-4">{{ page.header }}</header>
    <div class="flex min-h-screen">
      <aside class="w-1/4 bg-gray-200 p-4">{{ page.sidebar_first }}</aside>
      <main class="flex-1 p-6" id="main-content">{{ page.content }}</main>
    </div>
    <footer class="bg-white p-4 mt-4 border-t">{{ page.footer }}</footer>
    <js-bottom-placeholder token="{{ placeholder_token }}">
  </body>
</html>
